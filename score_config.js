/**
 * score_config.js
 * -----------------------------------------------------
 * スコアリングの配点はこのファイルだけを編集すれば変更できます。
 * app.js 側はこのオブジェクトのキーを見てスコアを合算するだけなので、
 * 数値を変えても・項目を増減しても app.js を触る必要はありません
 * (項目を増やす場合は app.js 側で該当する判定関数も追加してください)。
 *
 * 合計が100点になるように設計されていますが、合計値のチェックは
 * 行っていないので自由に調整可能です。
 * -----------------------------------------------------
 */

module.exports = {
  // .jp / .co.jp / .or.jp / .ne.jp ドメイン
  jp_domain: 10,

  // ひらがな・カタカナ検出
  japanese_text: 10,

  // <html lang="ja">
  lang_ja: 10,

  // 株式会社・合同会社・有限会社・◯◯商店 等の事業者名検出
  business_name: 20,

  // 〒000-0000 / 都道府県 / 市区町村などの日本住所検出
  japanese_address: 20,

  // /contact ページの存在
  contact_page: 10,

  // /company ページの存在
  company_page: 5,

  // Googleマップ埋め込み検出
  google_map: 5,

  // /access ページの存在
  access_page: 5,

  // /business ページの存在
  business_page: 5,
};
